# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# vercel
.vercel

# dados locais do CRM (substituidos por Supabase em producao)
/.data

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (NUNCA versionar — contêm links de checkout e tokens)
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code (configuração local, não necessária no deploy)
.claude/

# documentos internos (contrato contém dados bancários, não versionar)
*.docx
_contrato_extraido.txt

.vercel

# video bruto (18MB, fica no institucional otimizado)
mistura_cabeca_pb.mp4

# compact-pro: marcador transitorio (state.md e journal.jsonl ficam versionados)
.context/PENDING_SAVE
